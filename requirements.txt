# ============================================================
# E-RECYCLO — Vercel Optimized (AI-Enabled LITE)
# ============================================================

# -- Core Django --
Django==4.2.7
asgiref==3.11.1
sqlparse==0.5.5

# -- Django Extensions --
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-filter==23.5

# -- Database --
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# -- Static Files --
whitenoise==6.6.0

# -- WSGI Server --
gunicorn==21.2.0

# -- Environment Configuration --
python-decouple==3.8

# -- Image Processing --
Pillow==10.1.0

# -- PDF Generation --
reportlab==4.0.7

# -- Password Hashing --
argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0

# -- HTTP Requests --
requests==2.31.0
certifi==2023.11.17
charset-normalizer==3.3.2
idna==3.6
urllib3==2.1.0

# -- Timezone --
pytz==2023.3.post1
tzdata==2023.3

# -- AI / ML — CPU ONLY (Required for Vercel 500MB Limit) --
# We use the CPU-specific index to avoid the 7GB CUDA download
--extra-index-url https://download.pytorch.org/whl/cpu
numpy==1.26.4
torch==2.1.2+cpu
torchvision==0.16.2+cpu
ultralytics==8.1.0
opencv-python-headless==4.8.1.78
typing_extensions==4.9.0
